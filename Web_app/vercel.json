{
  "version": 2,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/" }
  ],
  "env": {
    "FLASK_ENV": "production",
    "FLASK_APP": "app"
  },
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
    }
  ]
}
